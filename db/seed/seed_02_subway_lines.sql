-- =====================================================================
-- 지하철 노선 기반 관광 추천 서비스 (MetroTrip)
-- 초기 데이터 : 지하철 노선
--
-- 근거 문서 : 데이터베이스 명세서 V1.10
-- 대상 테이블 : subway_lines
-- 건수 : 2건
--
-- 선행 조건 : 없음 (마스터)
-- =====================================================================

USE metrotrip;

-- 재적재 시 아래 주석을 해제할 것
-- DELETE FROM subway_lines;
-- ALTER TABLE subway_lines AUTO_INCREMENT = 1;

-- 1호선은 경원선·서울1호선·경부선·경인선·장항선을 묶어 부르는 안내용 명칭이며,
-- 구로역에서 인천 방면과 신창 방면으로 물리적으로 분기한다.
-- 단일 station_order 로 표현할 수 없으므로 갈래별로 line_id 를 분리한다.

INSERT INTO subway_lines (line_id, line_name, line_number, display_order) VALUES
  (1, '1호선 (인천)', '1', 1),
  (2, '1호선 (신창)', '1', 2);
